# bookshelf-api

> [!NOTE]
> Change bun to npm if using nodejs

To install dependencies:

```bash
bun install
```

To run (development):

```bash
bun run dev
```

To build:

```bash
bun run build
```

To run the build:

```bash
bun run start
```
